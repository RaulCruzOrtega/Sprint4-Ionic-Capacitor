import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sprint4.ionic',
  appName: 'sprint4-ionic',
  webDir: 'www',
  server: {
     androidScheme: 'https'
  }
};

export default config;
